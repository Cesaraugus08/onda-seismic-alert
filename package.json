{
  "name": "onda-seismic-alert",
  "version": "1.0.0",
  "description": "Sistema de alerta sísmica en tiempo real para Venezuela con modo de emergencia SOS",
  "author": "Cesar Sarmiento",
  "license": "All Rights Reserved",
  "private": true,
  "scripts": {
    "start": "npx serve .",
    "build": "echo 'No build step needed for PWA'",
    "cap:sync": "npx cap sync",
    "cap:android": "npx cap open android",
    "cap:ios": "npx cap open ios",
    "cap:build:android": "npx cap build android",
    "cap:build:ios": "npx cap build ios"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/cli": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.0"
  }
}
